@page "/tickets"

<PageTitle>Tickets</PageTitle>
<h3>Tickets</h3>

@if (ticketViews == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Priority</th>
                <th>Deadline</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            <ForEach Items="ticketViews" Context="ticketView">
                <tr>
                    <td>@ticketView.Title</td>
                    <td>@ticketView.Description</td>
                    <td>@ticketView.Priority</td>
                    <td>@ticketView.Deadline.ToString(format: "dd-MMM (yyyy)")</td>
                    <td>@ticketView.Status</td>
                </tr>
            </ForEach>
        </tbody>
    </table>
}



